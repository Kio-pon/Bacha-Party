#include <iostream>
using namespace std;

// 3.4 Fibonacci (ii) [Not graded]: iterative version of fib(n) using a loop
// instead of recursion.
int main()
{
    long long n;
    cout << "Enter the number of term you want to find: ";
    cin >> n;

    // TODO: iteratively compute and print the nth Fibonacci number

    return 0;
}
